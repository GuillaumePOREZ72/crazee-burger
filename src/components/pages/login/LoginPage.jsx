import LoginForm from "./LoginForm";

/**
 * Render the login page containing the login form.
 *
 * @returns {JSX.Element} The page element that renders the LoginForm component.
 */
export default function LoginPage() {
  // affichage
  return (
    <div>
      <LoginForm />
    </div>
  );
}
